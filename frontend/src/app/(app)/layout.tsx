/**
 * App Layout — three-column structure.
 *
 * Uses AppShell which connects to data contexts
 * and passes data to panels via props.
 */

import { AppShell } from "@/components/app/app-shell"

export default function Layout() {
  return <AppShell />
}
